apply plugin: "java"

repositories{
	mavenCentral()
}

dependencies {
	compile "org.json:json:20090211"
}

sourceSets{
	main.java.srcDir "src/main"
}

jar {
	from configurations.compile.collect { zipTree it }
	manifest.attributes("Main-Class":"developer.swarnim.weather.temperature")
}
